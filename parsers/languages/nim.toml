schema_version = 1
language = "nim"
revision = 1

[lexical]
line_comments = ["##", "#"]
doc_comments = ["##"]
block_comment = ["#[", "]#"]

[calls]
exclude = ["if", "elif", "while", "for", "return", "case", "of", "when", "discard"]
