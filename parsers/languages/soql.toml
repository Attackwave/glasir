schema_version = 1
language = "soql"
revision = 1

[lexical]
line_comments = ["--"]
doc_comments = ["--"]

[calls]
exclude = ["if", "else", "end", "for", "range", "with", "return", "true", "false", "null"]
