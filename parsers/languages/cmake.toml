schema_version = 1
language = "cmake"
revision = 1

[lexical]
line_comments = ["#"]
doc_comments = ["#"]

[calls]
exclude = ["if", "elseif", "else", "endif", "foreach", "endforeach", "while", "endwhile", "return", "set", "include", "function", "macro", "endfunction", "endmacro"]
