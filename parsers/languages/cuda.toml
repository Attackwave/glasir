schema_version = 1
language = "cuda"
revision = 1

[lexical]
line_comments = ["///", "//"]
doc_comments = ["///"]
block_comment = ["/*", "*/"]

[calls]
exclude = ["if", "else", "while", "for", "switch", "return", "sizeof", "new", "delete", "__syncthreads"]
