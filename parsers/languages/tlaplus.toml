schema_version = 1
language = "tlaplus"
revision = 1

[lexical]
line_comments = ["\\*"]
doc_comments = ["\\*"]

[calls]
exclude = ["IF", "THEN", "ELSE", "MODULE", "EXTENDS", "VARIABLES", "CONSTANTS", "LET", "IN"]
