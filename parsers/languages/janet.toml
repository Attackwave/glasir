schema_version = 1
language = "janet"
revision = 1

[lexical]
line_comments = ["#"]
doc_comments = ["#"]

[calls]
exclude = ["if", "when", "unless", "cond", "let", "do", "fn", "var", "set", "while", "each", "defn", "def", "defmacro"]
