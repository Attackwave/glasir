schema_version = 1
language = "gdscript"
revision = 1

[lexical]
line_comments = ["#"]
doc_comments = ["##"]

[calls]
exclude = ["if", "elif", "else", "for", "while", "match", "return", "pass", "var", "const", "signal", "enum"]
